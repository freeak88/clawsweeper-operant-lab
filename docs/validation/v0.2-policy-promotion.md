# v0.2 Policy Promotion Validation

Release marker:

- Tag: `v0.2-policy-promotion`
- Commit: `320a5684de4b50de8499837316b2f10a17a7bcbd`
- Node: `v24.15.0`
- pnpm: `10.33.2`

Passed:

- `pnpm run build`
- `pnpm run build:all`
- `node --test test/policy-rfc.test.ts`
- `node --test test/priority-engine.test.ts`
- `node --test test/review-memory.test.ts`
- `node --test test/model-routing.test.ts`
- `node --test test/adaptive-scheduler.test.ts`
- `node --test test/confidence-engine.test.ts`
- `node --test test/policy-promotion.test.ts`
- Targeted `oxlint`
- Targeted `oxfmt --check`

`pnpm run check` currently stops at `check:active-surface` because
`scripts/check-active-surface.ts` flags existing `Clownfish` and
`ProjectClownfish` strings in that checker file.

`pnpm test` builds successfully, then reports observed full-suite failures in
this local Windows/Node 24 validation run:

- CRLF assertions
- Workflow regex mismatch
- Codex subprocess spawn `EPERM`
- `pnpm ENOENT` in validation subprocess
- State-root publish assertions
- One repair hydration assertion

These full-suite failures are recorded as observed local validation results.
They are not claimed to be caused by v0.2.

Safety boundary:

- No scheduler behavior was changed.
- No apply behavior was changed.
- No automerge behavior was changed.
- No repair dispatch behavior was changed.
- No GitHub mutation behavior was changed.
